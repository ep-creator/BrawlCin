venv/**
venv/
.venv/

__pycache__/
*.pyc
*.pyo
*.pyd

# Pasta onde o app do Claude deposita arquivos entregues no chat
Claude outputs/

# pytest
.pytest_cache/

# Mapa antigo, substituído por assets/mapa.tmx. Os arquivos foram removidos do
# repositório mas continuam no disco porque esta sessão não tem permissão de
# exclusão. Estão ignorados para que um `git add -A` não os traga de volta.
# Apague os dois e remova estas linhas.
assets/mapa_brawl.tmx
assets/Map (10).png
